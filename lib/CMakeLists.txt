# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-nut
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND nut_sources
    nut_source_impl.cc)

set(nut_sources
    "${nut_sources}"
    PARENT_SCOPE)
if(NOT nut_sources)
    message(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT nut_sources)

########################################################################
# Find libavformat / libavutil / libavcodec (raw NUT demuxing; no decode
# contexts are created, libavcodec is only used for codec descriptors).
########################################################################
find_package(PkgConfig REQUIRED)
pkg_check_modules(FFMPEG IMPORTED_TARGET libavformat libavutil libavcodec)
if(NOT FFMPEG_FOUND)
    message(
        FATAL_ERROR
            "gr-nut requires the FFmpeg libavformat/libavutil/libavcodec development files.\n"
            "Debian/Ubuntu: sudo apt install libavformat-dev libavutil-dev libavcodec-dev\n"
            "Fedora: sudo dnf install ffmpeg-devel")
endif()

# boost::this_thread::interruption_requested() is used to make blocking pipe
# reads abort promptly on flowgraph stop (GR work threads are boost threads).
find_package(Boost REQUIRED COMPONENTS thread)

add_library(gnuradio-nut SHARED ${nut_sources})
target_link_libraries(gnuradio-nut gnuradio::gnuradio-runtime PkgConfig::FFMPEG
                      Boost::thread)
target_include_directories(
    gnuradio-nut
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>)
set_target_properties(gnuradio-nut PROPERTIES DEFINE_SYMBOL "gnuradio_nut_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-nut PROPERTIES INSTALL_NAME_DIR
                                                    "${CMAKE_INSTALL_PREFIX}/lib")
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
gr_library_foo(gnuradio-nut)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_nut_sources)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-nut)

if(NOT test_nut_sources)
    message(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_nut_sources)

foreach(qa_file ${test_nut_sources})
    gr_add_cpp_test("nut_${qa_file}" ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file})
endforeach(qa_file)
